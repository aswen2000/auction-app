info:
  name: GET - List listings
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:5173/api/listings?page=1&pageSize=5
  params:
    - name: page
      value: "1"
      type: query
    - name: pageSize
      value: "5"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
